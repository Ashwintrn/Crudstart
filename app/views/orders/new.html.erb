
<%= form_with model: @order,
local: true do |form| %>
<% if @order.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@order.errors.count, "error") %> prohibited
        this order from being saved:
      </h2>
      <ul>
        <% @order.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<p>
    <%= form.hidden_field :customer_id %><br>
    <%= form.hidden_field :customer_id %>
  </p>
 
  <p>
    <%= form.label :item_id %><br>
    <%= form.collection_select(:item_id, Item.order(:id,:name), :id ,:name)  %>
  </p>
 
 <p>
    <%= form.label :quantity %><br>
    <%= form.text_field :quantity %>
  </p>

  <p>
    <%= form.submit %>
  </p>
<% end%>
<%= link_to 'Back', list_order_path(current_customer) %>
  